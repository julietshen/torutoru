export type Phrase = {
  id: string;
  english: string;
  gujarati_script: string;
  romanized: string;
  category: string;
  audio_url?: string;
};

export type Word = {
  id: string;
  english: string;
  gujarati_script: string;
  romanized: string;
  category: string;
  audio_url?: string;
}; 